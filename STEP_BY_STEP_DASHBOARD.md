# 📸 스텝별 Dashboard 설정 가이드

## 단계별 스크린샷 가이드

### Step 1: Cloudflare Dashboard 접속

1. 브라우저에서 https://dash.cloudflare.com/ 열기
2. 로그인

### Step 2: Pages 프로젝트 찾기

1. 왼쪽 사이드바에서 **"Workers & Pages"** 클릭
   ```
   📁 Domain
   📁 Analytics
   📁 Workers & Pages  ← 여기!
   📁 ...
   ```

2. **"Pages"** 탭 클릭
   ```
   [ Workers ] [ Pages ]  ← 여기!
   ```

3. 프로젝트 목록에서 **"coindrop-kr"** 클릭

### Step 3: Settings 탭 열기

프로젝트 페이지 상단 탭 메뉴:
```
[ Overview ] [ Deployments ] [ Settings ] [ Functions ] [ Custom domains ]
                                     ↑
                                  여기 클릭!
```

### Step 4: Builds & deployments 섹션 찾기

Settings 페이지에서 아래로 스크롤:
```
Settings
├── General
├── Environment variables
├── Builds & deployments  ← 여기를 찾으세요!
├── Preview deployments
└── ...
```

### Step 5: 빌드 설정 입력

**Builds & deployments** 섹션에서:

1. **Build command** 입력란에:
   ```
   npm ci && npm run build
   ```

2. **Build output directory** 입력란에:
   ```
   .next
   ```

3. **Node.js version** 드롭다운에서:
   ```
   18
   ```

4. **Root directory**는 비워두기 (또는 `/`)

### Step 6: 저장

1. 페이지 하단의 **"Save"** 버튼 클릭
2. 성공 메시지 확인:
   ```
   ✅ Settings saved successfully
   ```

### Step 7: 재배포

1. 상단 **"Deployments"** 탭 클릭
2. 가장 최근 배포 항목 찾기
3. **"Retry deployment"** 또는 **"Retry build"** 버튼 클릭

## 확인 방법

배포가 시작되면:
1. Deployments 페이지에서 진행 상황 확인
2. 배포 로그에서 다음을 확인:
   ```
   ✅ Running build command: npm ci && npm run build
   ✅ npm ci 완료
   ✅ npm run build 완료
   ```

## 문제 해결

### "Builds & deployments" 섹션이 보이지 않아요
- Settings 페이지를 끝까지 스크롤하세요
- 또는 브라우저에서 Ctrl+F (또는 Cmd+F)로 "Builds" 검색

### "Save" 버튼이 비활성화되어 있어요
- 입력한 값이 올바른지 확인
- 페이지를 새로고침하고 다시 시도

### 재배포 후에도 같은 에러가 나와요
- 빌드 로그를 확인하여 실제로 빌드가 실행되었는지 확인
- 설정이 제대로 저장되었는지 다시 확인

---

**⏱️ 예상 소요 시간: 3-5분**

지금 바로 시작하세요!

